<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 1.12 Maintainer: progcom Status: ready -->
  <refentry id="function.str-replace">
   <refnamediv>
    <refname>str_replace</refname>
    <refpurpose>
     발견한 모든 검색 문자열을 치환 문자열로 교체합니다.
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>설명</title>
     <methodsynopsis>
      <type>mixed</type><methodname>str_replace</methodname>
      <methodparam><type>mixed</type><parameter>search</parameter></methodparam>
      <methodparam><type>mixed</type><parameter>replace</parameter></methodparam>
      <methodparam><type>mixed</type><parameter>subject</parameter></methodparam>
      <methodparam choice="opt"><type>int</type><parameter>&amp;count</parameter></methodparam>
     </methodsynopsis>
    <para>
     <parameter>subject</parameter>에서 발견한 모든
     <parameter>search</parameter>를 주어진 <parameter>replace</parameter>
     값으로 치환한 문자열이나 배열을 반환합니다. 복잡한 치환 규칙이 필요하지
     않다면, <function>ereg_replace</function>나
     <function>preg_replace</function> 대신 이 함수를 사용하십시오.
    </para>
    <para>
     PHP 4.0.5부터, <function>str_replace</function>의 모든 인자를
     <type>array</type>로 지정할 수 있습니다.
    </para>
    <warning>
     <simpara>
      PHP 4.3.3 이전 버전에는 <parameter>search</parameter>와
      <parameter>replace</parameter> 인자에 모두 배열을 사용할 경우, 빈
      <parameter>search</parameter> 인덱스를 <parameter>replace</parameter>
      배열의 내부 포인터를 옮기지 않은 채 넘어가는 버그가 존재했습니다.
      <literal>PHP 4.3.3</literal>에서 수정했기에, 이 버그에 관련된 모든
      스크립트는 이전의 동작을 하려면 이 함수를 호출 하기 전에 빈 search값을
      제거해야 합니다.
     </simpara>
    </warning>
    <para>
     <parameter>subject</parameter>가 배열이면, 검색 및 치환을
     <parameter>subject</parameter>의 모든 원소에서 수행하고, 배열을
     반환합니다.
    </para>
    <para>
     <parameter>search</parameter>와 <parameter>replace</parameter>가
     배열이면, <function>str_replace</function>는 각각의 배열에서 가져온 값을
     <parameter>subject</parameter>의 검색과 치환에 사용합니다.
     <parameter>replace</parameter>가 <parameter>search</parameter>보다 적은
     값을 가지면, 나머지 치환값은 빈 문자열을 사용합니다.
     <parameter>search</parameter>가 배열이고 <parameter>replace</parameter>가
     문자열이면; 치환 문자열을 모든 <parameter>search</parameter> 값에
     사용합니다.
    </para>
    <para>
     <example>
      <title><function>str_replace</function> 예제</title>
      <programlisting role="php">
<![CDATA[
<?php
// 결과: <body text='black'>
$bodytag = str_replace("%body%", "black", "<body text='%body%'>");

// 결과: Hll Wrld f PHP
$vowels = array("a", "e", "i", "o", "u", "A", "E", "I", "O", "U");
$onlyconsonants = str_replace($vowels, "", "Hello World of PHP");

// 결과: You should eat pizza, beer, and ice cream every day
$phrase  = "You should eat fruits, vegetables, and fiber every day.";
$healthy = array("fruits", "vegetables", "fiber");
$yummy   = array("pizza", "beer", "ice cream");

$newphrase = str_replace($healthy, $yummy, $phrase);

// PHP 5.0.0부터 사용할 수있는 count 인자 사용
$str = str_replace("ll", "", "good golly miss molly!", $count);
echo $count; // 2
?>
]]>
      </programlisting>
     </example>
    </para>

    &note.bin-safe;

    <note>
     <simpara>
      <literal>PHP 5.0.0</literal>부터 매치하고 치환한 횟수를 참조로 전달하는
      <parameter>count</parameter>로 반환할 수 있습니다.
      <literal>PHP 5.0.0</literal> 이전에는 이 인자를 사용할 수 없습니다.
     </simpara>
    </note>
    <para>
     참고:
     <function>str_ireplace</function>,
     <function>substr_replace</function>,
     <function>ereg_replace</function>,
     <function>preg_replace</function>,
     <function>strtr</function>.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
