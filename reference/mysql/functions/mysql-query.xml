<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 1.15 Maintainer: pcraft Status: ok -->
  <refentry xml:id="function.mysql-query" xmlns="http://docbook.org/ns/docbook">
   <refnamediv>
    <refname>mysql_query</refname>
    <refpurpose>MySQL 질의를 전송</refpurpose>
   </refnamediv>

   <refsect1 role="description">
    &reftitle.description;
    <methodsynopsis>
     <type>resource</type><methodname>mysql_query</methodname>
     <methodparam><type>string</type><parameter>query</parameter></methodparam>
     <methodparam choice="opt"><type>resource</type><parameter>link_identifier</parameter></methodparam>
    </methodsynopsis>
    <para>
<function>mysql_query</function>는 (<parameter>link_identifier</parameter>와 연관된 
데이터베이스 서버로) 질의를 전송한다.    
     <function>mysql_query</function> sends a query (to the currently
     active database on the server that's associated with the
     specified <parameter>link_identifier</parameter>).
    </para>
   </refsect1>

   <refsect1 role="parameters">
    &reftitle.parameters;
    <para>
     <variablelist>
      <varlistentry>
       <term><parameter>query</parameter></term>
       <listitem>
        <para>
SQL 질의.
        </para>
        <para>
세미콜론으로 끝나지 않는 질의 문자열.
        </para>
       </listitem>
      </varlistentry>
      &mysql.linkid.description;
     </variablelist>
    </para>
   </refsect1>

   <refsect1 role="returnvalues">
    &reftitle.returnvalues;
    <para>
SELECT, SHOW, DESCRIBE, EXPLAIN 구문으로
<function>mysql_query</function>를 호출하면
성공할 경우 <type>resource</type>를,
실패하면 &false;를 반환한다.
    </para>
    <para>
UPDATE, DELETE, DROP 등을 실행할 경우 성공하면 &true;를
실패하면 &false;를 반환한다.
    </para>
    <para>
반환되는 결과 리소스(resource)는 <function>mysql_fetch_array</function>와 같은
결과 테이블을 다루는 함수들에 전달하여 데이터에 접근할 수 있다.
    </para>
    <para>
SELECT 구문으로 부터 얼마나 많은 행이 있는지 알기 위해서는
<function>mysql_num_rows</function>를,
DELETE, INSERT, REPLACE, UPDATE 구문으로 변경된 행의 개수를 알기 위해서는
<function>mysql_affected_rows</function>를 사용한다.
    </para>
    <para>
질의에 의해 참조되는 테이블에 접근을 허용되지 않은 사용자에 의해
<function>mysql_query</function>가 실패하면 &false;를 반환한다.
    </para>
   </refsect1>

   <refsect1 role="examples">
    &reftitle.examples;
    <para>
     <example>
      <title>잘못된 질의</title>
      <para>
다음 질의는 문법적으로 오류가 있어서 
<function>mysql_query</function>는 실패하고,
&false;를 반환한다.
      </para>
      <programlisting role="php">
<![CDATA[
<?php
$result = mysql_query('SELECT * WHERE 1=1');
if (!$result) {
    die('Invalid query: ' . mysql_error());
}

?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <example>
      <title>유효한 질의</title>
      <para>
다음 질의는 유효한 질의로서 <function>mysql_query</function>는
<type>resource</type>를 반환한다.
      </para>
      <programlisting role="php">
<![CDATA[
<?php
// This could be supplied by a user, for example
$firstname = 'fred';
$lastname  = 'fox';

// Formulate Query
// This is the best way to perform a SQL query
// For more examples, see mysql_real_escape_string()
$query = sprintf("SELECT firstname, lastname, address, age FROM friends WHERE firstname='%s' AND lastname='%s'",
    mysql_real_escape_string($firstname),
    mysql_real_escape_string($lastname));

// Perform Query
$result = mysql_query($query);

// Check result
// This shows the actual query sent to MySQL, and the error. Useful for debugging.
if (!$result) {
    $message  = 'Invalid query: ' . mysql_error() . "\n";
    $message .= 'Whole query: ' . $query;
    die($message);
}

// Use result
// Attempting to print $result won't allow access to information in the resource
// One of the mysql result functions must be used
// See also mysql_result(), mysql_fetch_array(), mysql_fetch_row(), etc.
while ($row = mysql_fetch_assoc($result)) {
    echo $row['firstname'];
    echo $row['lastname'];
    echo $row['address'];
    echo $row['age'];
}

// Free the resources associated with the result set
// This is done automatically at the end of the script
mysql_free_result($result);
?>
]]>
      </programlisting>
     </example>
    </para>
   </refsect1>

   <refsect1 role="seealso">
    &reftitle.seealso;
    <para>
     <simplelist>
      <member><function>mysql_connect</function></member>
      <member><function>mysql_error</function></member>
      <member><function>mysql_real_escape_string</function></member>
      <member><function>mysql_result</function></member>
      <member><function>mysql_fetch_assoc</function></member>
      <member><function>mysql_unbuffered_query</function></member>
     </simplelist>
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
