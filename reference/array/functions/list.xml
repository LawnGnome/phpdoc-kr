<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 1.8 Maintainer: fuzzy74 Status: unknown  -->
  <refentry id="function.list">
   <refnamediv>
    <refname>list</refname>
    <refpurpose>
     배열처럼 변수들을 지정한다
    </refpurpose>
   </refnamediv>
   <refsect1>
    <title>설명</title>
     <methodsynopsis>
      <type>void</type><methodname>list</methodname>
      <methodparam rep="repeat"><type>mixed</type><parameter>...</parameter></methodparam>
     </methodsynopsis>
    <para>
     <function>array</function>처럼, 이 함수는 실제 함수가 아니고
     언어 구조이다. <function>list</function>는 한번의 조작으로 배열 목록을
     지정하기 위해 사용된다.
    </para>
    <note>
     <para>
      <function>list</function>는 숫자 배열에서만 작동하고 
      0부터 시작하는 숫자 인덱스로 가정한다.
     </para>
    </note>
    <para>
     <example>
      <title><function>list</function> 예제코드</title>
      <programlisting role="php">
<![CDATA[
<?php

$info = array('coffee', 'brown', 'caffeine');

// 모든 변수를 목록화한다
list($drink, $color, $power) = $info;
print "$drink is $color and $power makes it special.\n";

// 그들중 일부를 목록화한다
list($drink, , $power) = $info;
print "$drink has $power.\n";

// 또는 세번째 값으로만 건너띈다
list( , , $power) = $info;
print "I need $power!\n";

?>
]]>
      </programlisting>
     </example>
    </para>
    <para>
     <example>
      <title><function>list</function>의 사용예</title>     
      <programlisting role="php">
<![CDATA[
<table>
 <tr>
  <th>Employee name</th>
  <th>Salary</th>
 </tr>

<?php

$result = mysql_query ("SELECT id, name, salary FROM employees",$conn);
while (list ($id, $name, $salary) = mysql_fetch_row ($result)) {
    print (" <tr>\n".
           "  <td><a href=\"info.php?id=$id\">$name</a></td>\n".
           "  <td>$salary</td>\n".
           " </tr>\n");
}

?>

</table>
]]>
      </programlisting>
     </example>
    </para>
    <warning>
     <para>
      <function>list</function>는 가장 우측 매개변수에서 시작하는 값들을 지정한다.
      일반적인 변수들을 사용하면, 이 동작에 대해서 신경 쓸 필요가 없다.
      그러나 인덱스를 갖는 배열에서 사용한다면
      왼쪽에서 오른쪽으로 <function>list</function>에서 쓰여진것과 같이
      배열안의 인덱스 순서로 되는것이라고 보통 기대할수 있으나, 그렇지 않다.
      역순으로 지정이 된다.
     </para>
    </warning>
    <para>
     <example>
      <title>배열 인덱스를 사용하는 <function>list</function> 사용예</title>
      <programlisting role="php">
<![CDATA[
<?php

$info = array('coffee', 'brown', 'caffeine');

list($a[0], $a[1], $a[2]) = $info;

var_dump($a);

?>
]]>
      </programlisting>
      <para>
       다음과 같이 출력된다 (이 원소들의 순서는 <function>list</function> 
       문법안에서 쓰여진 순서와 대비된다는 것에 주의.):
      </para>
      <screen>
<![CDATA[
array(3) {
  [2]=>
  string(8) "caffeine"
  [1]=>
  string(5) "brown"
  [0]=>
  string(6) "coffee"
}
]]>
      </screen>
     </example>
    </para>
    <para>
     <function>each</function>, <function>array</function> 
     <function>extract</function> 참고.
    </para>
   </refsect1>
  </refentry>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
