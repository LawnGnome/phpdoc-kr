<?xml version="1.0" encoding="utf-8"?>
<!-- EN-Revision: 1.7 Maintainer: progcom Status: ready -->
<section xml:id="pcre.constants" xmlns="http://docbook.org/ns/docbook">
 &reftitle.constants;
 &extension.constants;
 <table>
  <title>PREG 상수</title>
  <tgroup cols="2">
   <thead>
    <row>
     <entry>상수</entry>
     <entry>설명</entry>
    </row>
   </thead>
   <tbody>
    <row>
     <entry>PREG_PATTERN_ORDER</entry>
     <entry>
      $matches[0] 배열은 패턴 전체를 가지고, $matches[1] 배열이
      처음 괄호에 들은 서브패턴의 문자열을 가지도록 결과를 지정합니다.
      이 플래그는 <function>preg_match_all</function>에서만 사용합니다.
     </entry>
    </row>
    <row>
     <entry>PREG_SET_ORDER</entry>
     <entry>
      $matches[0] 배열은 매치된 첫번째 집합을 가지고, $matches[1]는
      두번째 집합을 가지도록 결과를 지정합니다.
      이 플래그는 <function>preg_match_all</function>에서만 사용합니다.
     </entry>
    </row>
    <row>
     <entry>PREG_OFFSET_CAPTURE</entry>
     <entry>
      <constant>PREG_SPLIT_OFFSET_CAPTURE</constant>의 설명을 보십시오.
      이 플래그는 <literal>PHP</literal> 4.3.0부터 사용할 수 있습니다.
     </entry>
    </row>
    <row>
     <entry>PREG_SPLIT_NO_EMPTY</entry>
     <entry>
      이 플래그는 <function>preg_split</function>가 비어있지 않은 조각만을
      반환하게 합니다.
     </entry>
    </row>
    <row>
     <entry>PREG_SPLIT_DELIM_CAPTURE</entry>
     <entry>
      이 플래그는 <function>preg_split</function>가 구분자 안의 괄호로
      둘러싸인 표현식도 잡아내게 합니다. <literal>PHP</literal> 4.0.5부터
      사용할 수 있습니다.
     </entry>
    </row>
    <row>
     <entry>PREG_SPLIT_OFFSET_CAPTURE</entry>
     <entry>
      이 플래그를 설정하면, 매치하는 모든 문자열 요소의 시작 위치를 함께
      반환합니다. 매치된 문자열을 <literal>0</literal>으로, 문자열 시작 위치를
      <literal>1</literal>로 가지는 배열을 원소로 가지는 배열을 반환합니다. 이
      플래그는 PHP 4.3.0부터 사용할 수 있고,
      <function>preg_split</function>에서만 사용합니다.
     </entry>
    </row>
   </tbody>
  </tgroup>
 </table>
</section>

<!-- Keep this comment at the end of the file
Local variables:
mode: sgml
sgml-omittag:t
sgml-shorttag:t
sgml-minimize-attributes:nil
sgml-always-quote-attributes:t
sgml-indent-step:1
sgml-indent-data:t
indent-tabs-mode:nil
sgml-parent-document:nil
sgml-default-dtd-file:"../../../manual.ced"
sgml-exposed-tags:nil
sgml-local-catalogs:nil
sgml-local-ecat-files:nil
End:
vim600: syn=xml fen fdm=syntax fdl=2 si
vim: et tw=78 syn=sgml
vi: ts=1 sw=1
-->
